import { InativarDirective } from './inativar.directive';

describe('InativarDirective', () => {
  it('should create an instance', () => {
    const directive = new InativarDirective();
    expect(directive).toBeTruthy();
  });
});
